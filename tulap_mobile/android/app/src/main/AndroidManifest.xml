<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Network & Internet for API communication & sync -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- Camera & Microphone for Geotag Evidence, Video & Receipt Scanning -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <!-- Lokasi (foreground SAJA - Bagian 72 spesifikasi: "Tidak ada
         pelacakan berkelanjutan/background"). Dibutuhkan geolocator
         untuk watermark geotag & indikator GPS di kamera, dan untuk
         layar Lokasi mandiri. SENGAJA TIDAK menambahkan
         ACCESS_BACKGROUND_LOCATION - aplikasi ini tidak pernah melacak
         lokasi di latar belakang. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- Push Notification (FCM) - wajib dideklarasikan eksplisit untuk
         Android 13+ (API 33), diminta saat runtime lewat
         FirebaseMessaging.requestPermission() setelah sesi login
         terbentuk (lihat PushNotificationService), bukan langsung saat
         app dibuka. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <application
        android:label="Tulap.id"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <!-- Impeller (renderer default Flutter sejak versi terbaru) punya
                 bug kompatibilitas yang sudah dikenal luas dengan preview
                 kamera (camera_android_camerax) di sejumlah GPU/driver Android
                 tertentu - preview live tampil hitam/terpotong jadi
                 sepotong kecil di atas, PADAHAL foto yang benar-benar
                 diambil tetap tersimpan dengan benar (capture jalur lain,
                 tidak lewat pipeline render preview). Dikonfirmasi persis
                 kasus ini di perangkat fisik Realme RMX3834. Skia (renderer
                 lama) tidak punya masalah ini - dimatikan di sini per
                 rekomendasi resmi Flutter untuk app yang pakai kamera berat. -->
            <meta-data
              android:name="io.flutter.embedding.android.EnableImpeller"
              android:value="false"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Peta Sebaran Lokasi (Beranda) - google_maps_flutter. Nilai
             ${mapsApiKey} disuntik dari android/local.properties lewat
             manifestPlaceholders di build.gradle.kts, TIDAK PERNAH
             hard-code di sini. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${mapsApiKey}" />
        <!-- Push Notification (FCM) - channel & ikon default dipakai
             saat app di background/terminated dan sistem Android
             menampilkan notifikasi tray otomatis dari payload
             `notification` (tanpa kode tambahan). ic_launcher dipakai
             sebagai default pragmatis - belum ada aset ikon monokrom
             khusus notifikasi. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="tulap_default_channel" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/ic_launcher" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
