# URL backend NestJS (tulap_backend) - dipakai server-side saja (Route
# Handlers & Server Components), TIDAK PERNAH diekspos ke browser -
# karena itu prefix-nya bukan NEXT_PUBLIC_.
BACKEND_API_URL=http://localhost:3000
